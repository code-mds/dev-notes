* Install WANDisco repo package (for git >= 2.x)
```
yum install http://opensource.wandisco.com/centos/7/git/x86_64/wandisco-git-release-7-2.noarch.rpm
```

* Install GIT using yum
```
sudo yum install git
```

* Check Installation
```
git --version
```